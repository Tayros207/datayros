# Copia este archivo a .env para desarrollo local (.env está en .gitignore).
# En producción, esta variable se configura en el panel de Cloudflare Pages
# — no se sube ningún archivo. Ver README.md.

ANTHROPIC_API_KEY=
