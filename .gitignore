# Secretos — NUNCA subir
.env
.env.local
.env.*.local
.dev.vars

# Dependencias (por si más adelante se agregan)
node_modules/

# Cache y build de Cloudflare / Wrangler
.wrangler/
.mf/
dist/
.cache/

# Logs
*.log
npm-debug.log*

# Sistema operativo y editores
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Herramientas locales. El sitio es estático y se despliega tal cual: si
# package.json viaja al repo, Cloudflare Pages puede intentar un build que
# no existe y romper el despliegue.
package.json
package-lock.json

# Maquetas de trabajo y material fuente: se revisan en local, no se publican.
/preview/
img/Captura desde *.png
